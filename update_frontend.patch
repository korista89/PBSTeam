--- frontend/src/app/picture-word/page.tsx
+++ frontend/src/app/picture-word/page.tsx
@@ -258,18 +258,21 @@
     const renderStudentPanel = () => (
         <div style={{ width: '220px', flexShrink: 0 }}>
             <div style={cardStyle}>
-                <div style={{ fontWeight: 700, color: '#1e3a8a', marginBottom: '12px', fontSize: '0.95rem' }}>📚 학급 선택</div>
-                <select
-                    value={selectedClassId}
-                    onChange={e => setSelectedClassId(e.target.value)}
-                    style={{ width: '100%', padding: '8px', borderRadius: '8px', border: '1px solid #e2e8f0', marginBottom: '16px', fontSize: '0.9rem' }}
-                >
-                    {availableClasses.map(c => (
-                        <option key={c.id} value={c.id}>{c.name} ({c.id})</option>
-                    ))}
-                </select>
+                {isAdmin && (
+                    <>
+                        <div style={{ fontWeight: 700, color: '#1e3a8a', marginBottom: '12px', fontSize: '0.95rem' }}>📚 학급 선택</div>
+                        <select
+                            value={selectedClassId}
+                            onChange={e => setSelectedClassId(e.target.value)}
+                            style={{ width: '100%', padding: '8px', borderRadius: '8px', border: '1px solid #e2e8f0', marginBottom: '16px', fontSize: '0.9rem' }}
+                        >
+                            {availableClasses.map(c => (
+                                <option key={c.id} value={c.id}>{c.name} ({c.id})</option>
+                            ))}
+                        </select>
+                    </>
+                )}
 
-                <div style={{ fontWeight: 700, color: '#1e3a8a', marginBottom: '12px', fontSize: '0.95rem' }}>👤 학생 선택</div>
+                <div style={{ fontWeight: 700, color: '#1e3a8a', marginBottom: '12px', fontSize: '0.95rem' }}>👤 학생 선택 {isAdmin ? '' : `(${availableClasses.find(c => c.id === selectedClassId)?.name || ''})`}</div>
                 <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
                     {classStudents.length === 0 && (
                         <div style={{ color: '#94a3b8', fontSize: '0.85rem', padding: '8px', textAlign: 'center' }}>
@@ -291,15 +294,8 @@
                         </button>
                     ))}
                 </div>
-
-                <button
-                    onClick={() => setShowAddStudent(true)}
-                    style={{ marginTop: '12px', width: '100%', padding: '8px', border: '1px dashed #94a3b8', borderRadius: '8px', background: 'transparent', color: '#64748b', cursor: 'pointer', fontSize: '0.85rem' }}
-                >
-                    + 학생 추가
-                </button>
             </div>
         </div>
     );
 
